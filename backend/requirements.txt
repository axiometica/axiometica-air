# ─────────────────────────────────────────────────────────────────────────────
# Runtime dependencies — installed in the production image.
# Development tools (pytest, black, mypy, etc.) live in requirements-dev.txt.
# ─────────────────────────────────────────────────────────────────────────────

# Web Framework (Modernized for 2026 Core Compatibility)
fastapi>=0.115.0
uvicorn>=0.35.0
python-multipart>=0.0.12
python-jose==3.5.0          # CVE-2024-33663 CRITICAL: algorithm confusion — fixed 3.4.0
passlib==1.7.4
bcrypt==4.0.1
python-dotenv>=1.1.0

# Database
sqlalchemy==2.0.23
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.0

# Async & Background Jobs
celery==5.3.4
redis==5.0.1
flower==2.0.1

# Data validation (Upgraded to satisfy modern LLM / Agent frameworks)
pydantic>=2.12.0
pydantic-settings>=2.5.2

# WebSocket
websockets>=15.0.1

# HTTP Client
httpx>=0.28.1
aiohttp==3.14.1             # fixes 21 CVEs disclosed June 2026 — see GHSA advisories for aio-libs/aiohttp

# Graph Database (CMDB)
neo4j==5.15.0

# LLM Integration (OpenAI & modern partners)
openai>=1.40.0,<2.0.0
anthropic>=0.40.0,<1.0.0
requests==2.33.0

# Slack integration
slack-sdk>=3.26.0

# Security: explicit pins to override transitive dependency CVEs
jaraco.context==6.1.2       # CVE-2026-23949: path traversal — fixed 6.1.0
wheel==0.47.0               # CVE-2026-24049: privilege escalation — fixed 0.46.2

# Secrets-at-rest encryption (connector/Slack/SMTP credentials, LLM API keys)
cryptography>=42.0.0

# Utilities (Brought up to standard for core typing constraints)
python-dateutil==2.8.2
pytz==2023.3
typing-extensions>=4.15.0
pyyaml>=6.0.2
anyio>=4.8.0
packaging>=25.0,<26.0

# Rate limiting
slowapi>=0.1.9
limits>=3.13.0



# Error tracking (FastAPI plugin adapts automatically)
sentry-sdk[fastapi]==1.45.1

# Structured JSON logging
python-json-logger==2.0.7

# Remote execution adapters
paramiko>=3.4.0        # SSH adapter
pyvmomi>=8.0.0         # vCenter Guest Operations adapter
kubernetes>=29.0.0     # Kubernetes adapter (in-cluster + out-of-cluster)
boto3>=1.34.0          # AWS SSM adapter + CloudWatch metrics
azure-mgmt-compute>=30.0.0   # Azure Run Command adapter
azure-mgmt-monitor>=6.0.0    # Azure Monitor metrics
azure-identity>=1.15.0        # Azure credential providers